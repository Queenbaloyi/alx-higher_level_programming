#!/bin/bash

# Get the Python file name from the environment variable
PYFILE=$1

# Check if the Python file exists
if [ ! -f "$PYFILE" ]; then
  echo "Python file does not exist: $PYFILE"
  exit 1
fi

# Run the Python file
python3 "$PYFILE"
